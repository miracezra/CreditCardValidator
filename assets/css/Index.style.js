import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex:1,
      alignItems: "center",
    },
    text:{
      margin:15,
      textAlign:"center",
      fontSize: 20,
      fontWeight: 'bold',
      color: 'rgb(84,173,192)',
  
    }
  })